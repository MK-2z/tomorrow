# 素质评价分收集系统 - 独立部署版

## 项目介绍

这是一个学生素质评价分收集与管理系统，支持学生在线填写评价、上传证明文件，管理员审查、导出 Excel。

**本版本已移除平台依赖，可完全独立部署，无需飞书授权。**

## 技术栈

- 前端：React 19 + Vite + TailwindCSS
- 后端：NestJS 10 + Drizzle ORM
- 数据库：PostgreSQL
- 部署：支持 Docker / Render / Railway 等

## 快速开始（本地运行）

### 1. 环境要求

- Node.js 18+
- PostgreSQL 14+

### 2. 配置数据库

创建 PostgreSQL 数据库，然后复制环境变量配置：

```bash
cp .env.example .env
```

编辑 `.env` 文件，修改数据库连接信息：

```
DATABASE_URL=postgresql://用户名:密码@localhost:5432/数据库名
```

### 3. 安装依赖

```bash
npm install
```

### 4. 启动开发服务器

```bash
npm run dev
```

- 前端：http://localhost:5173
- 后端：http://localhost:3000

### 5. 构建生产版本

```bash
npm run build
npm run start:prod
```

## 部署到 Render（推荐，免费）

### 1. 注册 Render 账号

访问 https://render.com 注册账号（可用 GitHub 登录）。

### 2. 创建 PostgreSQL 数据库

1. 点击 "New" → "PostgreSQL"
2. 填写数据库名称（如 quality-eval-db）
3. 选择免费层（Free）
4. 点击 "Create Database"
5. 创建完成后，复制 "Internal Database URL"

### 3. 部署 Web 服务

1. 点击 "New" → "Web Service"
2. 选择 "Build and deploy from a Git repository"（需要先把代码推送到 GitHub）
   - 或者选择 "Deploy an existing image from a registry"
3. 配置：
   - **Build Command**: `npm install && npm run build`
   - **Start Command**: `node dist/server/main.js`
   - **Instance Type**: Free
4. 环境变量：
   - `DATABASE_URL`: 填入上一步复制的数据库 URL
   - `NODE_ENV`: production
   - `SUPER_ADMIN_ID`: 0001
   - `SUPER_ADMIN_PASSWORD`: admin123
5. 点击 "Create Web Service"

### 4. 访问网站

部署完成后，Render 会提供一个 URL（如 https://quality-eval.onrender.com），直接访问即可。

> 注意：Render 免费层会在 15 分钟无访问后休眠，首次访问可能需要等待 30 秒左右。

## 部署到 Railway

1. 注册 https://railway.app
2. 新建项目 → "Deploy from GitHub repo"
3. 添加 PostgreSQL 插件
4. 配置环境变量 `DATABASE_URL`
5. 配置 Build Command: `npm install && npm run build`
6. 配置 Start Command: `node dist/server/main.js`
7. 部署完成后绑定域名

## Docker 部署

### 构建镜像

```bash
docker build -t quality-eval .
```

### 运行容器

```bash
docker run -d \
  --name quality-eval \
  -p 3000:3000 \
  -e DATABASE_URL=postgresql://用户:密码@主机:5432/数据库 \
  -e SUPER_ADMIN_ID=0001 \
  -e SUPER_ADMIN_PASSWORD=admin123 \
  quality-eval
```

## 默认账号

| 角色 | 账号 | 密码 | 说明 |
|------|------|------|------|
| 超级管理员 | 0001 | admin123 | 教师登录端口，拥有全部权限 |
| 学生 | 学号 | 123456 | 首次登录自动注册，可修改密码 |

## 功能说明

### 学生功能
- 登录后填写个人信息（姓名、班级）
- 按表格填写素质评价分（正向分、负向分、自定义指标）
- 上传证明文件（与每条原因一一对应）
- 自动计算总分
- 查看自己的评价记录和状态（待审查/通过/打回）
- 被打回后可修改并重新提交

### 管理员功能（学生管理员）
- 审查工作台：查看所有学生的评价
- 逐指标确认/打回
- 标记待修改（精确到每条原因）
- 一键确认所有指标
- 删除评价记录

### 超级管理员功能（教师）
- 全部管理员功能
- 用户管理：搜索、筛选、批量删除用户
- 设置学生管理员角色
- 填写时间控制
- 操作日志查看
- 导出 Excel（可选择部分或全部）
- 删除用户

## 常见问题

### Q: 数据库表会自动创建吗？
A: 是的，应用启动时会自动执行 `server/database/init.sql` 创建表和默认管理员。

### Q: 证明文件存在哪里？
A: 当前版本证明文件以 base64 或 URL 形式存储在数据库的 JSON 字段中。如需对象存储，可自行配置。

### Q: 如何修改超级管理员密码？
A: 登录后在个人设置中修改，或通过环境变量 `SUPER_ADMIN_PASSWORD` 在首次启动时设置。

### Q: 学生忘记密码怎么办？
A: 超级管理员可以在用户管理中删除该用户，学生重新登录时会自动创建新账号（密码重置为 123456）。

### Q: 导出的 Excel 在哪里？
A: 在审查工作台中，选择学生后点击导出，会下载 Excel 文件。

## 项目结构

```
.
├── client/              # 前端代码
│   ├── src/
│   │   ├── api/         # API 接口
│   │   ├── components/  # 组件
│   │   ├── pages/       # 页面
│   │   ├── contexts/    # 上下文
│   │   ├── compat/      # 平台兼容层
│   │   └── utils/       # 工具函数
│   └── index.html
├── server/              # 后端代码
│   ├── modules/         # 业务模块
│   ├── database/        # 数据库配置和 schema
│   ├── common/          # 公共组件
│   ├── app.module.ts
│   └── main.ts
├── shared/              # 共享类型定义
├── Dockerfile
├── render.yaml
├── .env.example
├── package.json
└── README.md
```

## 技术支持

如遇到部署问题，请检查：
1. 数据库连接是否正确
2. 环境变量是否配置完整
3. Node.js 版本是否满足要求
4. 端口是否被占用

